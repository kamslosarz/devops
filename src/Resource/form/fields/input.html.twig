<p>
    {% if field.label %}
        <label for="size_1">{{ field.label }}</label>
    {% endif %}
    <input name="{{ form.name(field.name) }}"{% if field.value %} value="{{ field.value }}"{% endif %}{{ field.attributesAsString|raw }}>
</p>
