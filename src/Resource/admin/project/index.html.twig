{% extends 'admin/layouts/list.html.twig' %}

{% block title %}Project details{% endblock %}

{% block table %}
    <table id="projects-list">
        <th>Name</th>
        <th>Repository</th>
        <th>Status</th>
        <th>actions</th>
        <tbody class="main-table">
            {% for project in projects %}
            <tr>
                <td>
                    {{ project.name }}
                </td>
                <td>
                    {{ project.repository }}
                </td>
                <td>
                    {{ project.buildStatuses.first.status }}
                </td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}
